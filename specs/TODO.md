# TODO：后续产品迭代清单

本文档记录已讨论但尚未实现的功能。后续开发按优先级逐项推进，完成后再迁移到对应规格文档和实现代码。

## P0：当前体验修正

### 1. 信息源内容展示不完整（已完成）

问题：

- “AI 信息源”页面中，已添加来源只显示标题、类型和抓取状态。
- 用户无法查看来源正文内容，难以判断 LLM 提取结果是否可信。

建议：

- 在已添加来源卡片中增加“展开/收起正文”。
- 正文默认折叠，展开后显示前若干字，并提供完整查看区域。
- 对 URL 来源显示原始 URL、抓取状态和失败原因。

验收：

- 用户能在页面中查看已添加信息源正文。
- 长文本不会撑乱布局。
- 删除、提取因素、展开正文三个操作互不干扰。

状态：

- 已在来源卡片中增加 URL、抓取状态/失败原因和“查看正文”折叠区。
- 正文区域限制高度并允许滚动，长 URL 和长文本会自动换行。

### 2. 模拟次数可配置说明完善（已完成）

问题：

- 页面已支持配置模拟次数，但还需要在用户文档中说明其意义。

建议：

- README 增加“模拟次数如何影响结果”的说明。
- 报告中保留模拟次数，并提示次数越高随机波动越小、耗时越长。

验收：

- 普通用户能理解 `1000`、`5000`、`10000` 次模拟的取舍。

状态：

- README 已增加模拟次数说明，解释 `1000`、`5000`、`10000` 次模拟在速度和稳定性之间的取舍。
- 页面和报告流程会保留模拟次数，并提示次数越高随机波动越小、耗时越长。

### 3. 更新 README 以同步项目变化（已完成）

问题：

- README 需要持续反映当前项目状态，避免用户看到过期说明。
- 当前模型解释不够完整，尤其缺少 Elo、K 值、赛事权重、时间衰减和东道主加成的说明。
- 后续如果调整东道主加成，README 需要同步说明调整原因和默认值。

建议：

- 增加“模型如何工作”章节，用普通读者能理解的方式解释 Elo、K 值和 Poisson 模拟。
- 增加“东道主加成”说明，解释为什么美国、加拿大、墨西哥会获得额外加成。
- 在 README 中记录当前默认参数，并说明这些参数不是确定真理，可以随模型复盘继续调整。
- 同步说明完整历史数据、AI 影响因子入模、图形化报告、模拟次数配置等近期变化。

验收：

- 用户只读 README 就能理解项目当前功能边界。
- 用户能理解 Elo 和 K 值大致含义。
- 用户能理解东道主加成如何影响预测结果，以及后续调整的方向。

状态：

- README 已同步当前原型能力、完整历史数据、AI 影响因子入模、图形化报告和模拟次数配置。
- README 已增加“模型如何工作”章节，说明 Elo、K 值、赛事权重、时间衰减、东道主加成、Poisson 进球模拟和 Monte Carlo 模拟。

## P1：预测产品能力

### 4. 赛程表、近期赛程和完赛结果（大部分完成）

目标：

- 除预测概率外，应用也应作为世界杯赛程信息工作台。
- 用户可以查看最近几天的赛程安排、已完赛场次结果，并围绕比赛关注相关场外资讯。

建议：

- 引入公开可信赛程/赛果数据源，记录数据来源和更新时间。
- 在页面中增加“赛程”或“赛事进程”视图。
- 支持按日期、阶段、小组、球队筛选比赛。
- 已完赛比赛显示比分，并可同步到预测模型。
- 未开赛比赛显示时间、对阵、阶段和小组信息。
- 每场比赛关联天气、伤病、阵容、新闻等场外资讯入口。

验收：

- 用户能查看未来几天的比赛安排。
- 用户能查看已完赛场次比分。
- 赛程/赛果数据来源可追溯。
- 已完赛结果能与比分录入和预测模型状态保持一致。

状态：

- 已增加“赛事进程”视图，可查看 104 场完整赛程骨架。
- 小组赛显示具体球队；淘汰赛球队未定时显示签位和胜者路径。
- 小组赛已接入北京时间精确开球时间、官方场次编号和部分已完赛比分，整理在 `data/match_schedule_2026.json`。
- 当前已内置 16 场已完赛结果，最新包括比利时 1-1 埃及、伊朗 2-2 新西兰、西班牙 0-0 佛得角、沙特阿拉伯 1-1 乌拉圭。
- 赛程和比分来源已记录为 `Bing Sports / Wikipedia`：Bing 赛事页用于中国时区赛程和赛果，Wikipedia 分组赛页面用于场次编号和对阵结构交叉校验。
- 已支持按阶段/小组和状态筛选；`全部赛程` 和小组赛默认列表展示，淘汰赛可按晋级树或列表展示。
- 已录入或内置比分会在赛事进程视图中显示，并继续参与预测模型。
- 已引入 `APP_MODE=local|hosted` 的比分录入策略：local 模式允许用户录入未确认比分，hosted 模式不允许用户手动录入。
- 官方确认赛果与用户本地覆盖比分已分开处理；官方确认赛果只读，不显示“编辑比分”，不受“清空比分”影响。
- 尚未实现最近几天赛程聚合视图、城市/场馆字段、每场比赛的来源详情页，以及每场比赛关联天气、伤病、阵容、新闻等场外资讯入口。

### 5. 本地预测模型复盘与改进

目标：

- 系统性讨论当前本地预测模型是否合理，避免模型结果只是参数和数据偏差的产物。
- 明确哪些参数应暴露给用户，哪些应保持为专家设置。

待讨论问题：

- 32 强晋级规则已补齐第三名组合落位表：每组前二晋级，12 个小组第三中选出成绩最好的 8 个，再根据 495 种组合表填入 32 强签位；最佳第三名排序仍因缺少公平竞赛分、抽签等数据，使用 Elo 和随机抖动作为模拟兜底。
- Elo 初始分、K 值、赛事权重、时间衰减是否合理。
- 东道主加成是否过高，是否应按比赛地点或阶段区别处理。
- Poisson 进球模型是否需要加入进攻/防守拆分，而不是只使用单一 Elo 差。
- 小组赛和淘汰赛是否应使用不同的保守程度。
- 点球大战是否应加入独立参数，而不是只使用 Elo 期望胜率。
- 当前模型主要依赖历史比赛结果和已完赛比分，尚未系统性接入球队身价、主教练、最终名单、伤病、停赛、首发阵容、休息天数、旅途消耗、天气、战术克制、博彩公司赔率和球员级别实力数据。
- 是否纳入最终名单、伤病、休息天数、旅途消耗和战术克制等比赛上下文。
- 是否纳入博彩公司赔率或球员级别实力数据；如果纳入，如何处理数据可信度、时效性和版权/来源限制。
- 完整历史数据中球队名称、历史国家队变化、友谊赛质量是否会造成偏差。
- 是否需要提供“无主场加成”“低主场加成”“高主场加成”等对照预测。

建议：

- 增加模型参数说明页，解释当前默认值。
- 增加若干参数预设，而不是让普通用户直接面对所有公式参数。
- 输出模型诊断信息，例如前 20 名 Elo、东道主加成影响、数据来源范围。
- 用历史世界杯做回测，比较不同参数组合的合理性。
- 按层次扩展模型输入：
  - 模型底座：继续保留历史比赛、Elo、Poisson 和 Monte Carlo。
  - 球队静态实力层：球队身价、阵容深度、年龄结构、主教练和长期战术稳定性。
  - 赛前动态状态层：伤病、停赛、最终名单、预计首发、休息天数、旅途、天气和场地条件。
  - 市场校准层：在来源和版权可控的前提下，用博彩赔率校准胜平负概率，而不是直接替代本地模型。
  - LLM 解释层：负责整理来源、提取结构化影响因子和解释概率变化，不直接拍板输出最终概率。
- 将 README 中“局限与后续改进”的内容同步维护到 TODO，避免后续 README 重写时丢失。

验收：

- 能解释主要模型参数对预测结果的影响。
- 能生成至少一组对照预测，展示东道主加成或参数变化带来的差异。
- README 和报告中说明模型局限。

状态：

- 已新增 `docs/MODEL_EXPLANATION.md`，从数学和实现角度说明当前 Elo、Poisson、Monte Carlo、小组赛排序、淘汰赛模拟和 AI 影响因子入模方式。
- README 已增加“模型说明”入口，链接到模型说明文档。
- 尚未实现模型参数对照预测、历史世界杯回测、前 20 名 Elo 诊断、东道主加成敏感性分析，以及公平竞赛分/抽签等完整小组排名兜底数据。
- 尚未系统性接入球队身价、主教练、伤病、停赛、最终名单、预计首发、休息天数、旅途消耗、天气、赔率和球员级别实力数据；当前只能通过用户手动添加信息源和 LLM 影响因子做有限 Elo 微调。

### 6. 增加小组赛出线预测（已完成）

目标：

- 除夺冠和淘汰赛阶段概率外，单独展示每个小组的出线概率。

建议：

- 后端在模拟时记录每队小组排名概率：小组第 1、第 2、第 3、晋级 32 强。
- 前端在“正式分组”或“夺冠预测”中增加“小组出线预测”视图。
- 报告中加入小组出线图表。

验收：

- 用户能按小组查看每支球队的小组头名、前二、晋级概率。
- 已录入比分会影响小组出线概率。

状态：

- 后端已在模拟时累计每队小组头名、第二名、第三名、前二和晋级 32 强概率。
- 前端已在“夺冠预测”中增加“小组出线预测”卡片网格。
- HTML 报告已加入“小组出线预测”章节。

### 7. 按比赛阶段组织预测卡片（已完成）

目标：

- 除小组赛出线预测外，根据世界杯后续比赛安排，按赛事阶段展示不同预测内容。
- 让普通用户不需要理解 `r32`、`r16`、`qf`、`sf` 等缩写，也能按阶段阅读预测。

建议：

- 在“夺冠预测”中增加阶段卡片：
  - 小组赛：小组头名、前二、晋级 32 强概率。
  - 32 强：预计对阵、晋级 16 强概率。
  - 16 强：晋级八强概率。
  - 八强：晋级四强概率。
  - 四强：晋级决赛概率。
  - 决赛：夺冠概率。
- 阶段卡片应随赛事进展更新：已结束阶段显示结果，未结束阶段显示预测。
- 导出的 HTML 报告也按阶段组织，而不是只给总表。

验收：

- 用户能按比赛阶段浏览预测，不需要理解工程字段名。
- 已录入比分会改变当前阶段和后续阶段预测。
- 每张阶段卡片都显示预测时间、模拟次数和主要不确定性。

状态：

- “夺冠预测”页已增加阶段卡片，按小组赛、32 强、16 强、八强、四强、决赛展示关键概率。
- 阶段卡片会随重新预测更新，并显示模拟次数和不确定性提示。
- 导出的 HTML 报告已加入阶段化预测卡片。
- 32 强具体预计对阵仍需后续基于签位和赛事进程数据进一步展示。

### 8. 拆分预测结果页面（已完成）

目标：

- 当前“赛前夺冠预测”页同时承载夺冠概率、阶段预测卡片、阶段概率表和小组出线预测，信息密度偏高。
- 将预测结果按用户阅读任务拆分到更清晰的页面，避免单页拥挤。

建议：

- 将当前预测结果拆分为独立页面或标签：
  - 赛前夺冠预测：保留夺冠排行、进决赛概率和模型摘要。
  - 小组出线预测：集中展示每个小组的头名、前二、第三名和晋级 32 强概率。
  - 淘汰赛预测：结合淘汰赛签位和阶段概率，展示 32 强、16 强、八强、四强和决赛路径。
- 保留一个统一的“重新预测”入口，避免用户在多个页面重复触发同一件事。
- 导出的 HTML 报告也应按这些页面结构组织章节。

验收：

- 用户进入“赛前夺冠预测”时不会被小组出线和淘汰赛细节挤占主要阅读空间。
- 用户能从导航中直接进入“分组与出线”和“淘汰赛预测”。
- 三个预测页面共享同一次模拟结果、模拟次数、已完赛比分和 AI 影响因子状态。
- 页面拆分后，移动端和桌面端都不出现明显的内容堆叠或重复操作。

状态：

- 已在顶部导航中组织为“赛前夺冠预测”“分组与出线”“淘汰赛预测”三个页面。
- “赛前夺冠预测”页保留统一的重新预测入口，并聚焦夺冠概率排行、冠军概率和进决赛概率。
- “分组与出线”页预测前显示正式分组，预测后在同一组卡片中展示头名、前二、第三名和晋级 32 强概率，避免与正式分组页面重复。
- “淘汰赛预测”页已改为页内二级导航：默认显示“热门球队阶段概率”，并单独提供“32 强候选”“16 强候选”“深轮次候选”；深轮次候选合并展示八强、四强和决赛。
- 三个页面共享同一次预测结果、模拟次数、已完赛比分和 AI 影响因子状态。
- 导出的 HTML 报告仍按当前报告结构生成，后续可进一步同步为同样的三段式章节。

### 9. 增加实际结果和预测结果对比

目标：

- 随着比赛实际进行，用户需要知道模型此前的预测和真实结果之间的差异。
- 该功能用于复盘模型可信度，而不是只在赛前给出概率。

待讨论问题：

- 对比粒度如何设计：
  - 单场比赛：赛前胜平负概率、实际比分、模型是否判断正确。
  - 小组出线：赛前晋级概率和实际晋级结果对比。
  - 淘汰赛阶段：预测晋级概率和实际晋级路径对比。
  - 冠军/阶段概率：模型高概率球队是否按预期推进。
- 如何保存“当时的预测快照”，避免用赛后更新过的模型结果回看历史。
- 如何展示模型偏差：例如冷门、高估球队、低估球队、主场加成是否过强。
- 是否需要给每场比赛生成简短复盘说明，例如“模型低估了某队进攻表现”。

建议：

- 在每次重新预测时保存一个预测快照，记录时间、模拟次数、已知比分、AI 影响因子和模型参数。
- 增加“预测复盘”或“预测对比”页面：
  - 顶部显示整体命中情况和已完赛样本数。
  - 中部按比赛列出预测概率、真实比分、预测是否命中。
  - 下方按球队或阶段展示高估/低估情况。
- 对未完赛比赛保留预测，不计入命中率。
- 避免把概率判断简化为“对/错”：高概率事件失败也可能是合理波动，应展示概率而不是只展示结论。

验收：

- 用户能查看每场已完赛比赛的赛前预测概率和真实结果。
- 用户能看到整体预测复盘摘要，例如已完赛场次、热门命中、冷门场次。
- 预测快照不会被后续比分录入或模型参数变化覆盖。
- 页面能解释概率预测的局限，避免把低概率冷门简单视为模型错误。

状态：

- 已新增单场预测基础能力：每次重新预测会为 72 场小组赛已知对阵输出 `matchPredictions`，包含主胜/平局/客胜概率、双方期望进球、比分分布 Top N、实际比分和比分来源；赛事进程页面优先展示胜平负概率和期望进球，不把单一最可能比分作为主结论。
- 已新增预测快照持久化：每次成功重新预测都会写入带模型版本的快照文件，例如 `app_data/snapshots/20260615T143041117683-v1.json`；当前模型版本为 `v1`。快照内容不可修改，并记录已知比分、单场预测、阶段概率、小组出线概率、AI 因子和模型参数。
- 已增加快照磁盘保护：默认最多保留 200 个快照、总占用最多 50 MB，超过后删除最旧快照；可通过 `SNAPSHOT_MAX_FILES` 和 `SNAPSHOT_MAX_TOTAL_MB` 调整。
- 已新增“预测复盘”页面第一版：支持选择快照，展示完赛场次、胜负预测命中、赛前预测命中率、赛后复盘命中率、真实赛果概率、最大偏差和单场预测对比；所有已完赛且有单场概率的比赛都会参与复盘。
- 复盘中已区分两类预测：赛果未确定时生成的 `pre_result_prediction` 属于赛前预测，赛果已确定后再次用模型计算的 `post_result_explanation` 属于赛后复盘；两者分别展示统计，避免混淆“赛前命中”和“模型复盘解释能力”。
- 尚未实现球队/阶段维度的高估/低估分析、小组出线复盘和淘汰赛路径层面的复盘。

### 10. 支持 AI 辅助录入比分

目标：

- 减少用户逐场手动输入比分的负担。

建议：

- 用户粘贴赛果文本，或提供权威赛果 URL。
- 后端抓取正文后调用 LLM。
- LLM 提取待确认比分列表。
- 系统用正式赛程匹配球队和比赛。
- 用户确认后写入比分并重新预测。

验收：

- 能从一段文本中识别多场比分。
- 能识别中文队名、英文队名和常见别名。
- 低置信度或无法匹配的结果不会自动写入。
- 已有比分冲突时提示是否覆盖。

## P2：AI 内容能力

### 11. 生成世界杯参赛队伍介绍

目标：

- 通过 LLM 为每支参赛队生成普通读者能看懂的球队介绍。

内容范围：

- 最近历史战绩。
- 当家明星。
- 队员整体身价或估值区间。
- 关键优势、短板和不确定性。

建议：

- 用户提供权威数据源或允许系统使用配置的数据源。
- LLM 负责生成草稿，页面展示来源和生成时间。
- 对“身价”这类高时效数据必须标注来源和日期。

验收：

- 每支球队有可展开介绍卡片。
- 介绍内容有来源说明。
- 无来源时不得编造实时身价或球员状态。

## P3：工程结构和可维护性

### 12. 拆分 app.py 模块结构（已完成）

目标：

- 当前 `app.py` 同时承担 HTTP 服务、API 路由、赛程生成、信息源管理、LLM 调用、报告生成等多种职责，文件过大。
- 在继续增加预测复盘、页面拆分、AI 辅助录入等功能前，需要讨论并确定模块拆分方案。

待讨论问题：

- 采用轻量模块拆分，还是引入更正式的 Web 框架。
- 如何在不破坏当前本地启动方式的前提下拆分。
- API handler、数据读写、预测服务、LLM 服务、报告生成和赛程服务分别放在哪些文件。
- 拆分后如何保持测试和手动验证成本可控。
- 是否同步补充最小单元测试，避免拆分过程中行为回归。

建议：

- 先保持标准库 HTTP 服务不变，只做功能模块拆分。
- 初步拆分方向：
  - `app.py`：入口、HTTP server、请求分发。
  - `services/schedule.py`：分组、赛程、比分规范化和本地比赛状态。
  - `services/prediction.py`：预测调用、模型输入整理、预测结果本地化。
  - `services/sources.py`：信息源增删、URL 抓取、正文清洗。
  - `services/llm.py`：LLM 配置、连接测试、JSON 调用。
  - `services/report.py`：HTML 报告生成。
  - `storage.py`：JSON 读写、路径管理和运行目录。
- 先写拆分设计说明，再分小提交迁移，避免一次性大改。

验收：

- `python3 app.py` 启动方式保持不变。
- 现有 API 路径和前端行为保持兼容。
- 单个模块职责清晰，`app.py` 明显变薄。
- 拆分前后 `node --check web/app.js`、`python3 -m py_compile ...` 和核心 API 手动验证通过。

状态：

- 已按轻量模块拆分方案完成，不引入新的 Web 框架，`python3 app.py` 启动方式保持不变。
- `app.py` 已收敛为本地 HTTP server、静态文件服务、API 请求分发和应用状态。
- 已新增 `app_config.py` 集中管理路径和赛事常量。
- 已新增 `storage.py` 负责 JSON 读写和应用日志。
- 已新增 `services/schedule.py`、`services/prediction.py`、`services/sources.py`、`services/llm.py`、`services/report.py`、`services/teams.py`，分别承载赛程、预测、信息源、LLM、报告和球队本地化逻辑。
- 已将 `worldcup_ai_repro.py` 改名为 `worldcup_simulator.py`，使命令行入口和模块职责更贴近当前预测模拟功能。
- 已新增 `docs/PROJECT_STRUCTURE.md` 说明当前项目结构、模块职责、数据目录和主要调用链。
- 拆分后已通过 `python3 -m py_compile ...`、`node --check web/app.js`、CLI 预测烟测、Web App 预测烟测和报告 HTML 渲染烟测。
- 后续工程拆分重点转为 `worldcup_simulator.py` 内部模型模块化，例如 `model/elo.py`、`model/scoring.py`、`model/tournament.py` 和薄 CLI 入口。

## P4：网站化和多用户

### 13. 支持网站化部署

目标：

- 从本地单用户工具升级为可公开体验的网站版本。

建议：

- 前后端分离或保留轻量服务端渲染。
- 增加部署配置，例如 Dockerfile、环境变量和启动脚本。
- 将运行数据从本地 JSON 迁移到数据库。
- 增加只读演示模式，避免匿名用户写坏全局数据。

验收：

- 用户可以通过公网 URL 体验核心流程。
- 部署环境不暴露 API Key。
- 演示数据可重置。

### 14. 默认免费 LLM 与用户自定义 LLM

目标：

- 降低体验门槛，同时保留用户自带 OpenAI 兼容 API 的能力。

建议：

- 网站版本提供默认免费 LLM 配额。
- 用户可选择使用自己的 Base URL、API Key 和模型名。
- 后端按用户会话隔离 LLM 配置。
- 增加用量限制、错误提示和降级逻辑。

验收：

- 新用户无需配置即可体验基础 AI 功能。
- 用户自定义 LLM 不影响其他用户。
- 默认 LLM 有限流和滥用防护。

### 15. 多用户后台逻辑优化

目标：

- 当前本地内存和 JSON 文件状态只适合单用户，需要升级为多用户隔离。

建议：

- 引入会话 ID 或用户 ID。
- 比分、信息源、影响因子、报告按用户隔离。
- API Key 不落盘，或仅在用户明确授权时加密保存。
- 后台日志增加 request_id / session_id。

验收：

- 多个用户同时使用时，比分和信息源互不污染。
- 日志可以追踪单个用户的一次预测流程。

### 16. 添加项目地址（已完成）

目标：

- 在 README、网页页脚或关于页中展示项目地址。

待补信息：

- Git 仓库地址。
- 在线体验地址。
- 数据源地址。

验收：

- 用户能从页面或 README 找到项目主页和源码地址。

状态：

- 已在页面右上角状态区域增加 GitHub 图标链接，指向 `https://github.com/cuiguoke/world-cup-2026-predictor`。
- 页面仅显示 GitHub 图标，省略文字；通过 `aria-label` 保留可访问名称。
